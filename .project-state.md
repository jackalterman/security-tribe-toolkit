# Project State: security-tribe-toolkit

**Last Updated**: 2026-08-01 07:43:35
**Current Phase**: HAR-to-Flow Reconstruction: Chunk 1 (Detector Core)

## Progress Summary

Implemented src/services/harFlowDetector.ts per enhancement-har-flow-reconstruction.md. Pega-anchor detection (cookie + URL fragment heuristics) and OAuth2/OIDC state-correlated flow reconstruction are working. SAML entries are classified (hasUnreconstructedSamlTraffic) but not yet correlated into DetectedFlow objects — that's Chunk 3/4, pending xmlService.parseSamlResponse(). No UI wiring yet (Chunk 2). Project builds clean with `npm run build`.

## Completed Tasks

- [x] Chunk 1: harFlowDetector.ts - Pega SP anchor detection
- [x] Chunk 1: harFlowDetector.ts - OAuth2/OIDC state-based correlation
- [x] Chunk 1: harFlowDetector.ts - SAML entry classification (detection only)
- [x] Chunk 1: verified npm run build passes

## Current Task

- [/] Chunk 1 complete. Not yet started: Chunk 2 (FlowVisualizer initialFlowData prop + HarAnalyzer detection banner for OAuth/OIDC).

## Next Steps

1. Chunk 2: Add initialFlowData?: DetectedFlow prop to FlowVisualizer.tsx; when present, render real steps[] instead of getOauthSteps() mock generator; hardcode SP node as 'Pega'
2. Chunk 2: Wire App.tsx state (mirror decoderInitialData/onSendToDecoder pattern) to carry a DetectedFlow into FlowVisualizer
3. Chunk 2: Add detection banner + 'View in Flow Visualizer' button to HarAnalyzer.tsx, calling detectFlows() on harData
4. Chunk 3: xmlService.parseSamlResponse() to decode SAMLResponse/SAMLRequest and extract <saml:Issuer>, NameID, attributes
5. Chunk 3: Extend harFlowDetector.ts to correlate SAML flows via RelayState/timestamp using the new parser
6. Confirm real Pega ACS/callback URL pattern against an actual captured HAR (currently a loose fragment guess: /prweb/, PRAuth, PRRestService, PRServlet)

## Context

Working from D:\Scripts and Code\security-tribe-toolkit\enhancement-har-flow-reconstruction.md, which is the authoritative design doc and build-plan (Chunks 1-7). Key existing code the detector integrates with: services/har.ts (HarRoot/HarEntry types, existing per-entry authType tagging), services/jwtService.ts (jwtService.decode for JWT iss extraction), components/HarAnalyzer.tsx (PEGA_COOKIES const, currently local/not exported), components/FlowVisualizer.tsx (getOauthSteps/getSamlSteps mock generators, not yet modified), App.tsx (onSendToDecoder + activeView pattern to mirror for flow data). PEGA_COOKIES duplicated locally in harFlowDetector.ts rather than imported since HarAnalyzer.tsx doesn't export it.

## File Structure

- `src/services/harFlowDetector.ts`: NEW - Chunk 1 detector core: detectFlows(har) -> DetectedFlow[], hasUnreconstructedSamlTraffic(har) -> boolean

---

## Instructions for AI

When resuming work on this project:
1. Read this state file first using `get_project_state()`
2. Review the progress summary and completed tasks
3. Check for any blockers that need resolution
4. Continue with the next steps listed above
5. Update this state file after completing each major step
