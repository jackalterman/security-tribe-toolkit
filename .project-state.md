# Project State: security-tribe-toolkit

**Last Updated**: 2026-08-01 20:31:57
**Current Phase**: HAR-to-Flow Reconstruction: full-transcript rework complete and validated against real HAR data per user's exact spec

## Progress Summary

Per explicit user feedback (\"needs to start with first redirect... auth service name will be after PRAuth... every redirect and IdP call until the first !STANDARD where we have the Pega-AAT cookie\"), reworked harFlowDetector.ts from synthesizing a fixed 3-4-step summary per flow to reconstructing the FULL real transcript: every HAR entry between the Pega auth-service-launch request and the first request carrying a Pega-AAT session cookie, inclusive, restricted to the Pega+IdP hosts. Verified the exact boundary semantics against both real HARs with a Python probe before writing any TS (confirmed Pega-AAT appears as a request cookie, not response Set-Cookie, at a specific !STANDARD-path entry in each file) rather than guessing. Each step's diagram-node mapping (user/client/auth) is now computed directly by the detector per-entry (based on which host it hit) and carried on RealFlowStep.nodeFrom/nodeTo, replacing the old fragile fixed-index lookup table in FlowVisualizer.tsx -- this is more correct AND now scales to flows of any length. Re-validated end-to-end against both real HARs via the sandbox test harness: output now shows 10-11 steps for OIDC and 6 steps for SAML, matching the user's spec exactly (auth service launch -> every redirect/IdP call -> session established via Pega-AAT). Full project build passes. This was a substantial rework of the core detector file; FlowVisualizer.tsx needed only a small simplification since the new per-step node data made its old index-guessing table obsolete.

## Completed Tasks

- [x] Major rework per user feedback: flows now reconstruct the FULL real transcript instead of a synthesized 3-4 step summary
- [x] Added findFlowStartEntry(): locates the Pega PRAuth entry that names the auth service (e.g. '/PRAuth/app/<appName>/Auth0_OIDC/'), identified by last path segment not being a thread-ID (doesn't end in '*') and not being a !STANDARD hit; picks the closest such entry before the authorize/SAMLRequest entry so multi-flow HARs each get correct boundaries
- [x] Added findSessionEstablishedEntry(): locates the first !STANDARD-path request (after the callback) that carries Pega-AAT as a REQUEST cookie (not response Set-Cookie) -- confirmed exact entry index against both real HARs before implementing
- [x] Added buildStepTimeline(): builds the full chronological step list between flow-start and session-established (inclusive), filtered to {pegaHost, idpHost} to exclude unrelated third-party noise; every entry in range becomes a real captured RealFlowStep (title auto-derived by role: Auth Service Launch / IdP: <Authorize|Login|Resume|SAML Request> / Return to Pega (SP) / Pega Internal Redirect / Session Established)
- [x] Added nodeFrom/nodeTo directly onto RealFlowStep (computed by the detector from which host each entry actually hit: user<->client for pegaHost entries, user<->auth for idpHost entries, client<->auth only for the synthetic uncaptured token-exchange step) -- replaces the old fragile index-based REAL_OAUTH_STEP_NODE_PAIRS lookup table in FlowVisualizer.tsx entirely
- [x] Updated FlowVisualizer.tsx's stepsFromRealFlow() to consume step.nodeFrom/step.nodeTo directly instead of guessing from step index
- [x] Re-validated against both real HARs via the /home/claude/har-test harness: OIDC flow now shows 10 real captured steps (launch -> internal redirect -> authorize -> login GET -> login POST -> resume -> callback -> 3x internal normalization hops -> session established) + 1 inferred token-exchange step; SAML flow shows 6 real captured steps end-to-end, zero inferred/uncaptured steps needed since everything was present in that capture
- [x] Verified npm run build passes after the rework

## Current Task

- [/] Full-transcript reconstruction implemented, validated against both real HARs, and build-verified. Waiting on user's next round of feedback/testing, or explicit instruction to continue into remaining chunks (5: multi-flow UI polish: FlowVisualizer still has no in-app switcher if a HAR produces multiple DetectedFlow objects; 6: more IdP fingerprints beyond Okta/Entra/Auth0; 7: not yet reviewed).

## Next Steps

1. Wait for user's next test pass / fix list before continuing further
2. Chunk 5 (not started): FlowVisualizer has no in-app switcher for multiple flows in one HAR -- still unexercised since both real test HARs had exactly 1 flow each
3. Chunk 6 (partial): IDP_FINGERPRINTS covers Okta/Microsoft Entra/Auth0 only -- unvalidated against Okta/Entra/ADFS/PingFederate real captures
4. Chunk 7: not yet reviewed against the enhancement doc
5. Open question worth eventually asking the user: findFlowStartEntry's pattern is hardcoded to '/PRAuth/' -- confirm whether 'sometimes app/default or something' in their feedback implies a DIFFERENT launch path shape (not just a different app name) that the current regex might miss; both real captures used /PRAuth/app/<appName>/<AuthService>/ so this hasn't been tested against a '/PRAuth/app/default/...' or bare '/PRAuth/<AuthService>/' shape yet
6. The FlowVisualizer's 'Protocol Insight' side panel and provider-quirks-style UI chrome were written for the old 3-4-step model; worth a visual sanity pass now that real flows can have 10+ steps (e.g. does the step counter/prev-next UI still read well at that length) -- not yet checked in a live render, only validated at the data layer

## Context

User feedback verbatim: \"The har flow needs to start with first redirect, usually the auth service name will be after PRAuth or sometimes app/default or something. Then, every redirect and IdP call until the first !STANDARD where we have the Pega-AAT cookie.\" This was a significant scope change from the Chunk 1-3 design (which only showed 3-4 synthesized anchor steps per flow) to full-transcript reconstruction. Before implementing, verified the exact boundary entries against both real HARs with python (confirmed Pega-AAT first appears as a REQUEST cookie at HAR entry index 10 for OIDC / index 5 for SAML, both on !STANDARD paths; confirmed the auth-service-launch entry is index 0 in both real captures, identifiable by its last URL path segment not ending in '*' and not being !STANDARD). Re-synced the updated harFlowDetector.ts into /home/claude/har-test and re-ran against both real HARs to confirm the new full-transcript output before declaring this done -- output matches the user's spec exactly.

## File Structure

- `/home/claude/har-test/`: SANDBOX-ONLY test harness, kept in sync with harFlowDetector.ts/xmlService.ts/har.ts + jwtService.ts stub. Re-run via 'cd /home/claude/har-test && npx tsx run.ts' after any further detector changes, to re-validate against the same two real HAR files (still at /mnt/user-data/uploads/ this session) before declaring done.
- `src/services/harFlowDetector.ts`: Substantially reworked. New exports: FlowNode type. RealFlowStep now has required nodeFrom/nodeTo fields. New internal functions: findFlowStartEntry, findSessionEstablishedEntry, hasPegaAatRequestCookie, isStandardThreadPath, buildStepTimeline, describeStep, extractLastPathSegment, humanizeIdpSegment. buildOAuthFlow/buildSamlFlow now take a third `allEntries` param and delegate step construction to buildStepTimeline instead of hand-building 3-4 fixed steps.
- `src/components/FlowVisualizer.tsx`: stepsFromRealFlow() simplified to read step.nodeFrom/step.nodeTo directly; removed the old REAL_OAUTH_STEP_NODE_PAIRS index-based lookup table entirely.

---

## Instructions for AI

When resuming work on this project:
1. Read this state file first using `get_project_state()`
2. Review the progress summary and completed tasks
3. Check for any blockers that need resolution
4. Continue with the next steps listed above
5. Update this state file after completing each major step
